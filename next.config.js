/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.pexels.com',"rarible.com","cdn.pixabay.com"]
  },
}

module.exports = nextConfig
