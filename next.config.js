/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.pexels.com', "rarible.com", "i.pinimg.com"]
  },
}

module.exports = nextConfig
